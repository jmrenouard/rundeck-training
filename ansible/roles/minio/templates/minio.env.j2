# ==============================================================================
# Template de Fichier d'Environnement pour MinIO
# ==============================================================================
# Ce template Jinja2 génère le fichier .env pour la configuration du service MinIO.
# Les variables sont fournies par Ansible (voir defaults/main.yml).
#
# IMPORTANT: Pour un environnement de production, il est crucial de gérer
# ces secrets de manière sécurisée, par exemple avec Ansible Vault.
# ==============================================================================

# --- Identifiants d'accès Root ---
# Ces identifiants sont utilisés pour le premier accès à MinIO.
MINIO_ROOT_USER={{ minio_root_user }}
MINIO_ROOT_PASSWORD={{ minio_root_password }}

# --- Options de Lancement du Serveur ---
# Décommentez et configurez si vous avez besoin d'options supplémentaires.
# MINIO_OPTS="--address '{{ minio_listen_addr }}' --console-address '{{ minio_console_addr }}'"