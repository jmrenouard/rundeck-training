# Fichier d'exemple pour la configuration de l'environnement Docker.
# Copiez ce fichier en .env et personnalisez les valeurs si nécessaire.

# -- Configuration de Rundeck --
# URL publique de votre instance Rundeck.
# Assurez-vous que le port correspond à RUNDECK_PORT.
RUNDECK_GRAILS_URL=http://localhost:4440

# Port sur lequel Rundeck sera accessible sur la machine hôte.
RUNDECK_PORT=4440

# Répertoire sur la machine hôte pour stocker les données persistantes de Rundeck.
# Par défaut, un sous-répertoire 'rundeck-data' sera créé dans le répertoire courant.
RUNDECK_DATA_DIR=./rundeck-data

# -- Configuration de la base de données MySQL --
# Nom de la base de données pour Rundeck.
MYSQL_DATABASE=rundeck

# Utilisateur de la base de données pour Rundeck.
MYSQL_USER=rundeck

# Mot de passe pour l'utilisateur de la base de données.
# CHANGEZ CECI POUR UN MOT DE PASSE SECURISE.
MYSQL_PASSWORD=rundeckpassword

# Mot de passe root pour le serveur MySQL.
# CHANGEZ CECI POUR UN MOT DE PASSE SECURISE.
MYSQL_ROOT_PASSWORD=supersecretpassword

# Port sur lequel MySQL sera accessible sur la machine hôte.
MYSQL_PORT=3306

# Répertoire sur la machine hôte pour stocker les données persistantes de MySQL.
# Par défaut, un sous-répertoire 'mysql-data' sera créé dans le répertoire courant.
MYSQL_DATA_DIR=./mysql-data